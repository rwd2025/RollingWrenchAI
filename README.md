# Rolling Wrench AI Correct GitHub Pages Build

Upload these files to the ROOT of your GitHub repo.

This build is self-contained in `index.html`, so it will not break if GitHub misses `style.css`.

Files:
- index.html
- style.css
- app.js
- README.md
